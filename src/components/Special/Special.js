import React from 'react';

const Special = (props) => {
    const {ornaments}=props
    return (
        <div>
            <h1>Special One:{ornaments}</h1>
        </div>
    );
};

export default Special;